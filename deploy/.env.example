# Biến RUNTIME cho container trên server. Trên server:
#   cp .env.example .env   && chỉnh giá trị nếu cần
# Đặt cạnh docker-compose.yml (compose đọc qua `env_file: .env`).
NODE_ENV=production
API_BASE_URL=https://kimtv.net/java
API_ORIGIN=https://kimtv.net
API_TIMEOUT_MS=15000
